Genesis has been deployed as {{ include "genesis.fullname" . }}.

Key runtime values are controlled through .Values.config, .Values.database,
.Values.tls, and .Values.vault.

If you deployed with config.socketPath, no Service is created.
If you deployed over TCP/TLS, verify your TLS bundle path and Vault/DB inputs.
