name: Charts CI

on:
  pull_request:
    paths:
      - charts/**
      - .ct.yaml
      - .github/workflows/charts-ci.yml
  push:
    branches:
      - main
    paths:
      - charts/**
      - .ct.yaml
      - .github/workflows/charts-ci.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    outputs:
      has_changes: ${{ steps.changed.outputs.has_changes }}
      target_branch: ${{ steps.meta.outputs.target_branch }}
      mode: ${{ steps.meta.outputs.mode }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Helm
        uses: azure/setup-helm@v4

      - name: Setup chart-testing
        uses: helm/chart-testing-action@v2.7.0

      - name: Resolve chart test mode
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "mode=changed" >> "$GITHUB_OUTPUT"
            echo "target_branch=${{ github.base_ref }}" >> "$GITHUB_OUTPUT"
          else
            echo "mode=all" >> "$GITHUB_OUTPUT"
            echo "target_branch=main" >> "$GITHUB_OUTPUT"
          fi

      - name: Detect changed charts
        id: changed
        shell: bash
        env:
          TARGET_BRANCH: ${{ steps.meta.outputs.target_branch }}
          MODE: ${{ steps.meta.outputs.mode }}
        run: |
          set -euo pipefail

          if [[ "${MODE}" == "all" ]]; then
            echo "Push to main detected, running chart checks in --all mode."
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          changed="$(ct list-changed --target-branch "${TARGET_BRANCH}" --config .ct.yaml || true)"
          if [[ -n "${changed}" ]]; then
            echo "Changed charts:"
            echo "${changed}"
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          else
            echo "No chart changes detected."
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Run ct lint (all charts)
        if: steps.changed.outputs.has_changes == 'true' && steps.meta.outputs.mode == 'all'
        run: ct lint --all --config .ct.yaml

      - name: Run ct lint (changed charts)
        if: steps.changed.outputs.has_changes == 'true' && steps.meta.outputs.mode == 'changed'
        env:
          TARGET_BRANCH: ${{ steps.meta.outputs.target_branch }}
        run: ct lint --target-branch "${TARGET_BRANCH}" --config .ct.yaml

  install:
    runs-on: ubuntu-latest
    needs: lint
    if: needs.lint.outputs.has_changes == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Helm
        uses: azure/setup-helm@v4

      - name: Setup chart-testing
        uses: helm/chart-testing-action@v2.7.0

      - name: Setup Kind
        uses: helm/kind-action@v1.12.0

      - name: Run ct install (all charts)
        if: needs.lint.outputs.mode == 'all'
        run: ct install --all --config .ct.yaml

      - name: Run ct install (changed charts)
        if: needs.lint.outputs.mode == 'changed'
        env:
          TARGET_BRANCH: ${{ needs.lint.outputs.target_branch }}
        run: ct install --target-branch "${TARGET_BRANCH}" --config .ct.yaml
